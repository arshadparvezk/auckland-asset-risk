# Contributing

1. Create a feature branch.
2. Keep source endpoints and assumptions centralised in `config/model.yml`.
3. Add tests for every modelling change.
4. Run `pytest -q` and the full pipeline before opening a pull request.
5. Do not commit confidential asset values, personal information or unlicensed hazard data.
6. Describe whether new inputs are observed, modelled or illustrative.

